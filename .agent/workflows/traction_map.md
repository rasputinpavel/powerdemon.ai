---
description: Трекшн-карта — система экспериментов по сегментам для валидации гипотез
---

# Трекшн-карта

## Вход

- Прочитай `context.md` в папке бизнеса
- Прочитай `knowledge/icp_*.md` — нужны определённые сегменты
- Если ICP ещё нет — сначала предложи оператору выполнить `/icp_analysis`

## Шаг 1: Сформировать карту сегментов

Для каждого сегмента из ICP-анализа создай строку в карте:

```markdown
# Трекшн-карта: {BUSINESS_NAME}

| # | Сегмент | Гипотеза | Эксперимент | Статус | Результат |
|---|---------|----------|-------------|--------|-----------|
| 1 | {сегмент A} | {что проверяем} | {как проверяем} | 🟡 Не начат | — |
| 2 | {сегмент B} | {что проверяем} | {как проверяем} | 🟡 Не начат | — |
```

Статусы: 🟡 Не начат → 🔵 В работе → ✅ Подтверждён → ❌ Опровергнут → ⏸ Пауза

## Шаг 2: Определить эксперименты

Для каждого сегмента предложи 1-2 эксперимента. Для каждого:
1. Определи **гипотезу**: "Если X, то Y"
2. Определи **минимальный эксперимент**: что сделать, чтобы проверить?
3. Определи **метрику успеха**: конкретная цифра
4. Определи **бюджет** и **срок**

## Шаг 3: Создать карточки экспериментов

Для каждого эксперимента создай файл на основе шаблона `_templates/experiment_card.md`:

```
businesses/{business_name}/projects/{project}/experiments/{exp_name}.md
```

## Шаг 4: Согласовать с оператором

Покажи карту и карточки оператору. Спроси:
- С какого сегмента начать?
- Согласен с гипотезами и метриками?
- Какой бюджет на первый эксперимент?

## Шаг 5: Сохранить карту

Файл: `businesses/{business_name}/projects/{project}/traction_map.md`

Обнови `log.md` текущего проекта.

## Шаг 6: Обновление карты (при повторном вызове)

При следующем вызове `/traction_map` для того же проекта:
1. Прочитай существующую карту
2. Обнови статусы экспериментов
3. Добавь новые эксперименты если нужно
4. Предложи следующий эксперимент для запуска
